/**
 * Asset constants for the application.
 * To change the logo, replace the file at /public/assets/logo/logo.png
 * and update the path here if necessary.
 */
export const ASSETS = {
  LOGO: '/assets/logo/logo.png',
  // Fallback logo if the local one isn't found yet
  LOGO_FALLBACK: 'https://api.dicebear.com/7.x/identicon/svg?seed=mabisa',
};
